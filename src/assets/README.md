Place your profile photo here at `src/assets/profile.jpg` (or `profile.png`).

Recommended:
- Square crop (1:1), minimum 800x800px
- Use JPEG/PNG and name the file `profile.jpg` to match imports in the components
- After adding the file, restart dev server if hot-reload doesn't pick it up
